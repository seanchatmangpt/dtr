<!-- Generated by ggen-cli from ontology/dtr-spec.ttl -->
<!--
  Add this fragment to your project's <dependencies> section.
  DTR is a test-scoped library: it generates documentation during mvn test,
  and produces no runtime artifacts in production.
-->
<dependency>
    <groupId>io.github.seanchatmangpt</groupId>
    <artifactId>dtr-core</artifactId>
    <version>2026.3.0</version>
    <scope>test</scope>
</dependency>

<!--
  DTR requires Java 26 with preview features.
  Add this to your <build><plugins> section if not already present:

  <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-compiler-plugin</artifactId>
      <configuration>
          <release>26</release>
          <compilerArgs>
              <arg>--enable-preview</arg>
          </compilerArgs>
      </configuration>
  </plugin>
  <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-surefire-plugin</artifactId>
      <configuration>
          <argLine>--enable-preview</argLine>
      </configuration>
  </plugin>

  Generated test packages from this spec:
  - com.example.docs
-->
