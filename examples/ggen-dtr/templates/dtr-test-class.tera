/**
 * Copyright (C) 2013 the original author or authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License"); you may not
 * use this file except in compliance with the License. You may obtain a copy of
 * the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
 * WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
 * License for the specific language governing permissions and limitations under
 * the License.
 */
package {{ sparql_results[0]["?package"] }};

import io.github.seanchatmangpt.dtr.DocDescription;
import io.github.seanchatmangpt.dtr.DocSection;
import io.github.seanchatmangpt.dtr.DtrTest;
import io.github.seanchatmangpt.dtr.junit5.DtrExtension;
import org.junit.jupiter.api.AfterAll;
import org.junit.jupiter.api.MethodOrderer;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.TestMethodOrder;
import org.junit.jupiter.api.extension.ExtendWith;

/**
 * Generated by ggen-cli from ontology/dtr-spec.ttl.
 *
 * <p>This documentation test was scaffolded automatically. Each {@code @Test} method
 * corresponds to a {@code dtr:DocSection} in the ontology. Fill in the say* calls
 * below to document your feature, then run {@code mvnd verify} to produce
 * {@code target/docs/{{ sparql_results[0]["?className"] }}.md}.</p>
 *
 * <p>Regenerate at any time: {@code ggen sync} (idempotent — safe to re-run).</p>
 */
@ExtendWith(DtrExtension.class)
@TestMethodOrder(MethodOrderer.MethodName.class)
public class {{ sparql_results[0]["?className"] }} extends DtrTest {

{% set seen_sections = [] %}
{% for row in sparql_results %}
{% if row["?sectionTitle"] not in seen_sections %}
{% set_global seen_sections = seen_sections | concat(with=row["?sectionTitle"]) %}
    // ── Section {{ row["?order"] }}: {{ row["?sectionTitle"] }} ──────────────────────────────────

    @Test
    @DocSection("{{ row["?sectionTitle"] }}")
    @DocDescription({"{{ row["?description"] }}"})
    public void test0{{ row["?order"] }}_{{ row["?sectionTitle"] | lower | replace(from=" ", to="_") }}() {
        say("{{ row["?description"] }}");
{% set sayMethods = [] %}
{% for m in sparql_results %}{% if m["?sectionTitle"] == row["?sectionTitle"] and m["?sayMethod"] %}{% set_global sayMethods = sayMethods | concat(with=m["?sayMethod"]) %}{% endif %}{% endfor %}
{% if "sayCode" in sayMethods %}
        sayCode("""
                // TODO: add a representative code example
                """, "java");
{% endif %}
{% if "sayTable" in sayMethods %}
        sayTable(new String[][]{
            {"Property",  "Type",    "Default", "Description"},
            {"TODO_key",  "string",  "—",       "TODO: describe this setting"}
        });
{% endif %}
{% if "sayNote" in sayMethods %}
        sayNote("TODO: add a helpful note about this section.");
{% endif %}
{% if "sayWarning" in sayMethods %}
        sayWarning("TODO: document edge cases or production caveats here.");
{% endif %}
    }

{% endif %}
{% endfor %}
    @AfterAll
    static void afterAll() {
        finishDocTest();
    }
}
