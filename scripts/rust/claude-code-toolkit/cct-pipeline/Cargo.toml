[package]
name = "cct-pipeline"
version = "0.1.0"
edition = "2021"
description = "Multi-phase validation pipeline framework with JSON receipt — generalizes dx.sh"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
