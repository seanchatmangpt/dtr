# io.github.seanchatmangpt.dtr.jotp.MathsDocTest

## Table of Contents

- [Maths.sum() — int Overflow Avoided by long Result](#mathssumintoverflowavoidedbylongresult)
- [Maths.sum() — Adding Two Positive int Primitives](#mathssumaddingtwopositiveintprimitives)
- [Record Value Equality — equals() and hashCode()](#recordvalueequalityequalsandhashcode)
- [Maths.sum() — Commutativity](#mathssumcommutativity)
- [Output Record Equality](#outputrecordequality)
- [Record toString() — Human-Readable Representation](#recordtostringhumanreadablerepresentation)
- [Maths.sum() — Zero as an Identity Element](#mathssumzeroasanidentityelement)
- [Maths.sum() — Negative int Primitives](#mathssumnegativeintprimitives)
- [Maths.Input — Record Instantiation and Primitive Accessors](#mathsinputrecordinstantiationandprimitiveaccessors)
- [Maths.Output — Record with a long Primitive](#mathsoutputrecordwithalongprimitive)


## Maths.sum() — int Overflow Avoided by long Result

Because <code>Output.result</code> is a <code>long</code>, adding two <code>int</code> values near <code>Integer.MAX_VALUE</code> does <em>not</em> overflow — the addition is widened before overflow can occur.

| Check | Result |
| --- | --- |
| sum(Integer.MAX_VALUE, 1) = 2147483648 (no overflow) | `✓ PASS` |

| Check | Result |
| --- | --- |
| sum(Integer.MIN_VALUE, -1) = -2147483649 (no underflow) | `✓ PASS` |

## Maths.sum() — Adding Two Positive int Primitives

<code>sum()</code> accepts a <code>Maths.Input</code> record and returns a <code>Maths.Output</code> record whose <code>result</code> is the arithmetic sum of the two <code>int</code> components, widened to <code>long</code>.

| Check | Result |
| --- | --- |
| sum(10, 32) = 42 | `✓ PASS` |

| Check | Result |
| --- | --- |
| sum(1, 1) = 2 | `✓ PASS` |

| Check | Result |
| --- | --- |
| sum(100, 900) = 1000 | `✓ PASS` |

## Record Value Equality — equals() and hashCode()

Two records are equal when all their components are equal. This is value equality (structural), not reference equality.

| Check | Result |
| --- | --- |
| Input(5,7).equals(Input(5,7)) is true | `✓ PASS` |

| Check | Result |
| --- | --- |
| Input(5,7).equals(Input(5,8)) is false | `✓ PASS` |

| Check | Result |
| --- | --- |
| hashCode() is consistent with equals() | `✓ PASS` |

## Maths.sum() — Commutativity

Addition is commutative: <code>sum(x, y) == sum(y, x)</code>. This mirrors the mathematical property of the <code>+</code> operator.

| Check | Result |
| --- | --- |
| sum(13, 29) equals sum(29, 13) | `✓ PASS` |

## Output Record Equality

Output records are also compared by value.

| Check | Result |
| --- | --- |
| Output(100).equals(Output(100)) is true | `✓ PASS` |

| Check | Result |
| --- | --- |
| Output(100).equals(Output(200)) is false | `✓ PASS` |

## Record toString() — Human-Readable Representation

The auto-generated <code>toString()</code> includes the record name and all component names and values, which is useful for logging and debugging.

| Check | Result |
| --- | --- |
| Input(1,2).toString() contains component values | `✓ PASS` |

| Check | Result |
| --- | --- |
| Output(3).toString() contains result value | `✓ PASS` |

## Maths.sum() — Zero as an Identity Element

Adding zero to any integer returns that integer unchanged. Both <code>sum(0, n)</code> and <code>sum(n, 0)</code> yield <code>n</code>.

| Check | Result |
| --- | --- |
| sum(0, 0) = 0 | `✓ PASS` |

| Check | Result |
| --- | --- |
| sum(0, 7) = 7 | `✓ PASS` |

| Check | Result |
| --- | --- |
| sum(7, 0) = 7 | `✓ PASS` |

## Maths.sum() — Negative int Primitives

Records happily store negative <code>int</code> values. Adding a negative is equivalent to subtraction.

| Check | Result |
| --- | --- |
| sum(-5, 3) = -2 | `✓ PASS` |

| Check | Result |
| --- | --- |
| sum(-10, -10) = -20 | `✓ PASS` |

| Check | Result |
| --- | --- |
| sum(-1, 1) = 0 | `✓ PASS` |

## Maths.Input — Record Instantiation and Primitive Accessors

A Java record is a concise immutable data class. The compiler auto-generates a canonical constructor, accessor methods named after each component, plus <code>equals()</code>, <code>hashCode()</code>, and <code>toString()</code>.

Creating <code>new Maths.Input(3, 4)</code> stores two <code>int</code> primitives.

| Check | Result |
| --- | --- |
| input.x() returns the first int component: 3 | `✓ PASS` |

| Check | Result |
| --- | --- |
| input.y() returns the second int component: 4 | `✓ PASS` |

## Maths.Output — Record with a long Primitive

<code>Maths.Output</code> holds a single <code>long</code> field. Storing the sum as <code>long</code> prevents overflow when both <code>int</code> operands are at their maximum values.

| Check | Result |
| --- | --- |
| output.result() returns the long component: 42 | `✓ PASS` |

---
*Generated by [DTR](http://www.dtr.org)*
