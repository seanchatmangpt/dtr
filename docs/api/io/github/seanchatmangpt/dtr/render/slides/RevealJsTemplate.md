# `RevealJsTemplate`

> **Package:** `io.github.seanchatmangpt.dtr.render.slides`  

Reveal.js slide template for HTML5 presentation decks. Generates self-contained HTML5 using Reveal.js CDN. Supports speaker notes, code highlighting, and markdown content.

```java
public record RevealJsTemplate() implements SlideTemplate {
}
```

