# `MediumTemplate`

> **Package:** `io.github.seanchatmangpt.dtr.render.blog`  

Medium blog platform template. Medium accepts Markdown with custom JSON headers for API integration. Includes estimated reading time and publication metadata.

```java
public record MediumTemplate() implements BlogTemplate {
}
```

