# `LinkedInTemplate`

> **Package:** `io.github.seanchatmangpt.dtr.render.blog`  

LinkedIn platform template for article posts. LinkedIn articles are limited in markdown support and character limits. Formatting optimized for algorithm engagement (short paragraphs, numbered lists).

```java
public record LinkedInTemplate() implements BlogTemplate {
}
```

