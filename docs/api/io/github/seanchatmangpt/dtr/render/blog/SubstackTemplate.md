# `SubstackTemplate`

> **Package:** `io.github.seanchatmangpt.dtr.render.blog`  

Substack newsletter platform template. Substack uses plain Markdown with optional metadata headers. Newsletter-specific formatting for subscriber engagement.

```java
public record SubstackTemplate() implements BlogTemplate {
}
```

